spring:
  application:
    name: H2
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
      format_sql: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:./database/h2/db
    username: sa
    password: 123
